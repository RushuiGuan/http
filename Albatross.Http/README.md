# Albatross.Http

A companion library for `HttpClient` providing extensions and shared types for HTTP client code generation.

## Features

- Service exception types with HTTP context
- URL building extensions with query string support
- GZip response handling
- Multipart form data helpers
- Content type constants
