# Albatross.Http

A companion library for `HttpClient` providing resilience policies, extensions, and shared types for HTTP client code generation.

## Features

- Service exception types with HTTP context
- HttpClient extension methods
- Content type constants

## Installation

```bash
dotnet add package Albatross.Http
```

## Usage